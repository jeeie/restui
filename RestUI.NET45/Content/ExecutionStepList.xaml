<UserControl x:Class="RestUI.Content.ExecutionStepList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
 
    
    <Grid>
        <ListView Margin="2" Name="actionListView">
            <ListView.ItemTemplate>
                <DataTemplate x:Name="DataTemplateItem">
                    <StackPanel >
                        <!--Image Source="{Binding ??}" Height="32" Width="32" Canvas.Left="8" Canvas.Top="8"/-->
                        <TextBlock Text="{Binding Text}" FontSize="15"/>
                        <TextBlock Text="{Binding Status}" FontSize="12"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
